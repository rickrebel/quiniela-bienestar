# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]

# Directorio temporal de trabajo
.scratch/

# Base de datos SQLite
db/app.db
db/*.sqlite3

# Respaldos de BD (traen datos personales de los jugadores)
db/*.bak.sql
db/dump_full.json

# Archivos estáticos recolectados (collectstatic)
staticfiles/

# Tailwind: binario descargado y CSS compilado (artefactos de build;
# en deploy: `manage.py tailwind build` antes de collectstatic)
.django_tailwind_cli/
static/css/tailwind.css

# Capturas before/after de migraciones visuales
before*.png
after*.png

# Capturas y snapshots generados por Playwright (MCP)
.playwright-mcp/

# Entornos virtuales (si tienes uno)
venv/
.venv/

# Variables de entorno
.env

# no sé bien qué onda con este:
.idea/
.idea/misc.xml
richard/

